<h1>Editar perfil</h1>

<%= form_with model: @user_profile, local: true do |f| %>
  <%= f.label :name, 'Nome' %>
  <%= f.text_field :name %>
  <%= f.label :bio, 'Bio' %>
  <%= f.text_area :bio %>
  <%= f.submit 'Salvar' %>
<% end %>
